# 实验结论汇总

## 实验配置

所有实验基于 Alarm 网络 (37 节点, 46 边), n_samples=10000, 30 次 batch 运行。

---

## 一、max_sdiff 约束实验 (exp1-5, exp8-9)

**参数扫描**: max_sdiff = 20, 50, 100, 200, 500

| max_sdiff | Sdiff 实际最大值 | Sdiff P95 | MDL σ | 最优无环 MDL |
|:---------:|:--------------:|:---------:|:-----:|:-----------:|
| 20 | 16 | 8 | 607 | 153,859 |
| 50 | 14 | 9 | 627 | 153,814 |
| 100 | 14 | 10 | 573 | 153,832 |
| 200 | 15 | 9 | 572 | 153,844 |
| 500 | 15 | 9 | 601 | 153,846 |

**结论**: max_sdiff 从 20 变到 500，Sdiff 实际最大值始终在 14-16。MDL 惩罚项自然约束了结构差异——高 sdiff 的解 MDL 过差，被 Chebyshev 聚合自动淘汰。设置一个宽松的上界（如 50）即可，算法对此参数不敏感。

---

## 二、环约束实验 (exp6-7)

**对比**: max_cycle=6 (限界 BFS, 默认) vs max_cycle=37 (无界 DFS, 禁止所有环)

| max_cycle | 每代耗时 | 无环率 | Sdiff |
|:---------:|:-------:|:-----:|:-----:|
| 6 | 1.71s | 56% | ~18 |
| 37 | 0.63s | 100% | ~18 |

**结论**: 禁止所有环 (无界 DFS) 反而比限界 BFS 快 2.7x，同时保证 100% 无环解。原因是 BFS 开销取决于图的实际可达深度——有限制的 BFS 在有环图上探索更深，无限制的 DFS 在无环图上快速触底。**最终采用无界 DFS，删除 max_forbidden_cycle 参数。**

---

## 三、变异算子实验 (exp2)

**对比**: mutation_ops 2-6 (默认) vs 8-12

| 变异范围 | MDL σ | 效果 |
|:-------:|:-----:|:----:|
| 2-6 | ~600 | 正常收敛 |
| 8-12 | 2609 | MDL σ 爆炸 4.4x, 种群散乱 |

**结论**: 变异操作数过大 (8-12) 破坏交叉成果，算法退化为随机搜索。**2-6 是 Alarm 网络的最优范围。**

---

## 四、max_sdiff=60000 极端值实验 (exp6)

**对比**: max_sdiff=500 vs 60000（均超过理论上界 n×K+E_prior=198）

| max_sdiff | 初始 MDL mean | 初始 MDL σ | 初始 Sdiff max | 最终 MDL σ | 最终 Sdiff max |
|:---------:|:------------:|:----------:|:-------------:|:---------:|:-------------:|
| 500 | 154,714 | 625 | 15 | 601 | 15 |
| 60000 | 158,688 | 3,942 | 39 | 2,907 | 26 |

**根因**: `generate_initial_population` 中 `max_perturb = max(1, int(max_sdiff * (i + 1) / n_pop))`，扰动幅度直接受 max_sdiff 控制。max_sdiff=60000 → max_perturb 最高达 60000，初始个体被随机边操作严重破坏，MDL 质量极差。

**这不是约束太松的问题，而是初始种群生成的参考系错误**——扰动幅度应以先验图自身为尺度 (E_prior)，而非约束上限。修复方向: `max_perturb` 改用 `E_prior` 计算，与 `max_sdiff` 解耦。

**关键洞察**: 500 之所以和 60000 不同，不是因为 PF 上界不同（两者都远超 198），而是 500 恰好适配 Alarm 的规模（max_perturb ≤500 仍可控），60000 则失控。这也说明 MOEA/D 的初始种群并非越分散越好——分散出的解必须"有意义"，随机扰动过度的解只是噪声，反而扭曲 Chebyshev 归一化。

---

## 五、全局缓存优化 (代码演进)

**优化路径**:
1. MDLScore 独立 hash 缓存 `(node, parents) → MDL`
2. 删除 CompositeScore (dirty-node 机制从未被使用)
3. 合并为 MOEAD 层组合缓存 `(node, parents) → (MDL, sdiff)`

**效果**: batch_alarm (优化前, max_cycle=6) 17,100s → exp8 (优化后, max_cycle=6) 1,948s = **8.78x 加速**。单次评估从 17.1ms 降到 1.95ms。

---

## 六、交叉算子分析

**创新点**: 将 MOEA/D 的切比雪夫权重向量 λ 嵌入交叉内部驱动父集选择——使交叉"子问题感知"。同一对父代，不同子问题产生不同子代。这在 MOEA/D 文献中无先例。

**与已有工作对比**:
- Wong & Leung 2004: 单目标 merge 算子, 用原始 MDL 比较, 无权重向量
- Contaldi 2017: 随机选择父集 (50% 硬币), 非评分驱动
- 我们的: 多目标 + λ 驱动 + Chebyshev 聚合

**非创新部分**: 逐节点父集级重组 (Wong & Leung 2004 已有), 从空图构件 + 即时判环 (实现策略, 非算法创新)

---

## 七、论文可用的关键数据

| 发现 | 数据支撑 |
|------|---------|
| MDL 自然约束 Sdiff | 5 组实验, max_sdiff=20~500, 实际 Sdiff 始终 ≤16 |
| 禁止所有环 = 最优策略 | 100% 无环 + 最快 (0.63s/gen) |
| 全局缓存 8.78x 加速 | 17,100s → 1,948s |
| 子问题感知交叉 | 与单目标 merge 算子的本质区别 |
| 无需手动调 max_sdiff | 设 50 或自动计算 n×K+E_prior 均可 |

---

## 八、交叉算子对比实验

**实验设计**: 6 个网络 (sachs 11 节点 → andes 223 节点)，3 种交叉算子 (sequential / score-diff-sort / no-cycle-check)，每种 10 次 batch 运行。

### 8.1 score-diff-sort vs sequential

**唯一区别**: 节点处理顺序——seq 随机打乱，sds 按 Chebyshev 评分差 `|g1 - g2|` 降序排列。两者使用完全相同的缓存机制（`parent1_scores` / `parent2_scores` / `score_cache`）。

| 网络 | 节点数 | sequential | score-diff-sort | 差距 |
|------|:-----:|:----------:|:---------------:|:----:|
| sachs | 11 | 63s | 79s | 1.26x 慢 |
| insurance | 27 | 75s | 97s | 1.30x 慢 |
| alarm | 37 | 118s | 143s | 1.22x 慢 |
| hailfinder | 56 | 515s | 542s | ≈持平 |
| win95pts | 76 | 649s | 686s | ≈持平 |
| **andes** | **223** | **5,827s** | **4,106s** | **0.70x (快 30%)** |

**MDL 质量**: 所有网络两者几乎一致。混合非支配解中 seq/sds 各占约 50%——没有一方能支配另一方。

**种群结构 (andes)**: 
- seq: 种群平均边数 340, PF 最终大小 150
- sds: 种群平均边数 339, PF 最终大小 261
- 两者种群边数几乎相同，收敛速度也相似（均在 5-10 代内达到 95% PF）

**v2 复验** (andes 缩短 gen=300, munin1 新增 186 节点):

| 网络 | 节点 | Seq s/gen | SDS s/gen | Ratio | 显著性 |
|------|:---:|:---------:|:---------:|:-----:|:-----:|
| win95pts | 76 | 0.83s | 0.89s | 1.07x | *** |
| munin1 | 186 | 1.88s | 1.79s | 0.96x | 不显著 |
| **andes** | **223** | **3.86s** | **2.83s** | **0.73x** | *** |

**Andes 复现确认**: sds 在 223 节点上确实快 27%，非偶然。

**效应随节点数单调变化**: 76 节点 sds 更慢 → 186 节点持平 → 223 节点 sds 更快。约 200 节点是临界点。

**Profiling 根因** (per-call crossover 内部分解, andes 30 代):

| Phase | seq 早期 | sds 早期 | seq 后期 | sds 后期 |
|-------|:----:|:----:|:----:|:----:|
| get_parents | 0.52ms | 0.59ms | 0.55ms | 0.58ms |
| score+cheb | 0.57ms | 0.64ms | 0.05ms | 0.03ms |
| pre (gp+score+cheb) | — | 1.23ms | — | 0.61ms |
| **add_edge** | **1.99ms** | **1.58ms** | **3.28ms** | **1.20ms** |
| 总耗时 | 3.19ms | 4.26ms | 3.99ms | 2.57ms |
| n_diff | 95/223 | 95/223 | 3/223 | 1/223 |

差异 100% 来自 `add_edge` 内的 `_would_create_cycle` DFS。后期 seq 的 add_edge (3.28ms) 是 sds (1.20ms) 的 2.7 倍，其他阶段完全一致。

**机制**: SDS 按 Chebyshev 分差排序后，父集相同的节点（后期占 99%+）排在最后，保持原始节点索引顺序。Andes 节点索引遵循因果拓扑序（原因→结果），按此序添加边时子节点尚无后代 → DFS 瞬间触底返回 False。Seq 随机打乱破坏拓扑序 → 判环时 DFS 需遍历更深的后代路径 → 每次 `add_edge` 多花 ~2ms。9000 次交叉调用 × 2ms = 18s，恰好等于两者总耗时差。

**v3 验证** (andes gen=300, seq 删除 shuffle):

| 版本 | seq s/gen | sds s/gen | 谁更快 |
|------|:---------:|:---------:|:------:|
| v2 (shuffled) | 3.86s | 2.83s | sds 快 27% |
| **v3 (no-shuffle)** | **1.17s** | 1.87s | **seq 快 60%** |

去 shuffle 后 seq 加速 3.29x，地位彻底逆转。自然索引序 = 隐式拓扑序 → 判环 DFS 零成本。sds 的预计算反成瓶颈。

**结论**: sequential 模式（保持自然索引序）在所有规模上都是最优。score-diff-sort 不提升解质量且预计算开销使其永远慢于优化后的 seq，建议删除。no-cycle-check 在任何规模上都不可行。

**为什么排序策略不提升解质量**: 边阻塞率仅 0.06-0.65%（alarm 0.58%, andes 0.07%），其中能被不同节点顺序"拯救"的边更少。排序优势的前提（被阻塞的边足够多且对顺序敏感）不成立。但判环机制本身极其高效——用 0.5% 的边拒绝率阻止了 20% 的子代变含环图。每条被拒边都是"闭环边"，阻止它的 DFS 仅遍历 ~7 节点。

### 8.2 no-cycle-check — 不可行

| 网络 | 节点数 | sequential | no-cycle-check | 慢多少 |
|------|:-----:|:----------:|:--------------:|:------:|
| sachs | 11 | 63s | 81s | 1.29x |
| alarm | 37 | 118s | 174s | 1.48x |
| hailfinder | 56 | 515s | 940s | 1.82x |
| insurance | 27 | 75s | 168s | 2.24x |
| win95pts | 76 | 649s | 713s | 1.10x |
| **andes** | **223** | **97 min** | **>12h 未完成** | **∞** |

**MDL 质量**: 与 sequential 相当（insurance 上 nocc 的 best MDL 甚至略优: 98,062 vs 98,404，但差异在标准差范围内）。

**根因**: `_fix_cycles` 每次找环都从节点 0 重新扫描（`_iter_cycles()` 的复杂度为 O(n × edges)）。对 andes (223 节点)，nocc 构建的无环图边数接近 `n × max_parents = 223×7 = 1,561`，其中包含大量环。每代需删除数百条边，每次删边后重新全量扫描——这是 O(edges² × n) 级别的开销。

**结论**: no-cycle-check 在任何规模的网络上都不如 sequential。用户"大图上 nocc 更优"的假设被**否定**——即时判环（sequential）才是正确的策略，它利用 DAG 的部分序性质在构建过程中自然约束边数，避免事后修复的高昂代价。

### 8.3 总览

| 算子 | 小图 (<50节点) | 中图 (50-100) | 大图 (>200) |
|------|:------------:|:----------:|:--------:|
| sequential | **最优** | **最优** | 可用 (97min/run) |
| score-diff-sort | 稍慢 | 持平 | **最优** (68min/run) |
| no-cycle-check | 慢 1.3-2.2x | 慢 1.1-1.8x | 不可行 |
